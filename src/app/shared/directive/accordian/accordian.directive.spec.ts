import { AccordianDirective } from './accordian.directive';

describe('AccordianDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordianDirective();
    expect(directive).toBeTruthy();
  });
});
