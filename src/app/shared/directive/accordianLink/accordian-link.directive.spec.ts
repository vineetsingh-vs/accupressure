import { AccordianLinkDirective } from './accordian-link.directive';

describe('AccordianLinkDirective', () => {
  it('should create an instance', () => {
    // const directive = new AccordianLinkDirective();
    // expect(directive).toBeTruthy();
  });
});
