import { AccordianToggleDirective } from './accordian-toggle.directive';

describe('AccordianToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordianToggleDirective();
    expect(directive).toBeTruthy();
  });
});
